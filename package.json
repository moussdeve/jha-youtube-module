{
  "name": "youtube-search-module",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rmdir /s /q dist",
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npx serve .",
    "dev": "npm run watch & npm run serve"
  },
  "dependencies": {
    "@lit/context": "^1.1.6",
    "lit": "^3.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "serve": "^14.0.0",
    "typescript": "^5.0.0"
  }
}
